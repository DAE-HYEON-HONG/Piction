import React, {Component} from 'react';

class SearchItemList extends Component {
    render() {
        const { searchItems, onRemove } = this.props;

        return (
            <div>

            </div>
        );
    }
}